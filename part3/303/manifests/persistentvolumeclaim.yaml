apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  namespace: todo-project
  name: todo-volume-claim
spec:
  storageClassName: todo-pv
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Gi